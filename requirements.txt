# Databricks Apps: pip install -r requirements.txt
# Same pins as the Databricks Gradio template (gradio + huggingface_hub + pandas).
# cache-bust: 2026-04-05 — force reinstall if Apps skipped "Requirements have not changed"
gradio~=4.44.0
gradio-client==1.3.0
huggingface-hub~=0.35.3
pandas~=2.2.3
databricks-sdk
# New features dependencies
scikit-learn>=1.3.0
Pillow>=10.0.0
cryptography>=41.0.0
openai>=1.30.0
-e ".[rag,rag_embed,app,llm_openai,features]"

reportlab>=4.0.0  # PDF generation for chat history export
